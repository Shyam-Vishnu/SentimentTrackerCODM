# Copy to scripts/.env if running locally
OPENAI_API_KEY=your_openai_key_here
SUBREDDIT=CallOfDutyMobile
POST_LIMIT=50
# Optional: only include posts newer than this many hours (empty = no filter)
MAX_AGE_HOURS=72
